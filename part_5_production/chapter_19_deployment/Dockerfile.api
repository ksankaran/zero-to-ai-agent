# From: Zero to AI Agent, Chapter 19, Section 19.2
# File: Dockerfile.api
# Usage: docker build -f Dockerfile.api -t agent-api:v1 .

FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Expose the API port
EXPOSE 8000

# Run the API server
CMD ["uvicorn", "production_api:app", "--host", "0.0.0.0", "--port", "8000"]
